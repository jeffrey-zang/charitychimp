import './Navbar.scss'

const Navbar = () => {
    const pages = ['Home', 'Browse', 'Search', 'Post']

    return (
        <div id='nav'>
            Logo
        </div>
    )
}

export default Navbar;